
mdlDigitalAssetCatelogServer::DigitalAssetCatelogServer(input = input,output = output,session = session,dms_token = dms_token,erp_token=erp_token)
  

mdlDigitalAssetCatelogServer::LPsrcViewServer(input = input,output = output,session = session,dms_token = dms_token,erp_token=erp_token)
